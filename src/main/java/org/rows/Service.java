package org.rows;


/* NOT USED?
 public abstract class Service<T extends IModel<T>> implements IService<T> {
 
	private List<IController<T>> views = new ArrayList<IController<T>>();
	
	public void addView(IController<T> view)
	{
		this.views.add(view);
	}
	
	
	public List<IController<T>> getViews()
	{
		return this.views;
	}
	
	
	public void handle(Request request, Response response)
	{
	}
} */
